{% extends "base.html.twig" %}
{% block test_title %}
    <h2 style="margin:15px 0px 0px 0px">My notes</h2>
    <a href="new">
    <button type="button" class="btn btn-success" style="margin-bottom: 10px">Add note</button>
    </a>
{% endblock %}

{% block notes %}
    {% if notes %}



        <div class="row">
            <div class="column" style="width: 20%">
                <div class="card text-white mb-3" style="max-width: 18rem; background-color: red">
                    <div class="card-header">PHP</div>
                </div>
                {% for note in notes|filter(note => note.category == 'php') %}
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ note.name }}</h5>
                            <p class="card-text">{{ note.description }}</p>
                            <p class="card-text"><small class="text-muted">data</small></p>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="column" style="width: 20%">
                <div class="card text-white mb-3" style="max-width: 18rem; background-color: red">
                    <div class="card-header">SQL</div>
                </div>
                {% for note in notes|filter(note => note.category == 'sql') %}
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ note.name }}</h5>
                            <p class="card-text">{{ note.description }}</p>
                            <p class="card-text"><small class="text-muted">data</small></p>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="column" style="width: 20%">
                <div class="card text-white mb-3" style="max-width: 18rem; background-color: red">
                    <div class="card-header">Symfony</div>
                </div>
                {% for note in notes|filter(note => note.category == 'symfony') %}
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ note.name }}</h5>
                            <p class="card-text">{{ note.description }}</p>
                            <p class="card-text"><small class="text-muted">data</small></p>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="column" style="width: 20%">
                <div class="card text-white mb-3" style="max-width: 18rem; background-color: red">
                    <div class="card-header">Windows</div>
                </div>
                {% for note in notes|filter(note => note.category == 'windows') %}
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ note.name }}</h5>
                            <p class="card-text">{{ note.description }}</p>
                            <p class="card-text"><small class="text-muted">data</small></p>
                        </div>
                    </div>
                {% endfor %}
            </div>
            <div class="column" style="width: 20%">
                <div class="card text-white mb-3" style="max-width: 18rem; background-color: red">
                    <div class="card-header">Ubuntu</div>
                </div>
                {% for note in notes|filter(note => note.category == 'ubuntu') %}
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">{{ note.name }}</h5>
                            <p class="card-text">{{ note.description }}</p>
                            <p class="card-text"><small class="text-muted">data</small></p>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    {% endif %}
{% endblock %}